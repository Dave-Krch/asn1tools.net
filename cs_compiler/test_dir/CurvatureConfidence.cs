namespace test_namespace {
    enum CurvatureConfidence {
        onePerMeter-0-00002,
        onePerMeter-0-0001,
        onePerMeter-0-0005,
        onePerMeter-0-002,
        onePerMeter-0-01,
        onePerMeter-0-1,
        outOfRange,
        unavailable,
    }
}
